# Файловые ссылки — 2026-09-17

Пользователь попросил открывать ссылки на изображение/готовый exe обычным нажатием, а по ПКМ показывать меню с пунктом «Открыть в проводнике» и выделением файла.

## Поведение и устройство

- `src/Markdown.tsx` рисует доступные с клавиатуры ссылки. ЛКМ/Enter вызывает scoped `bridge.openPath`; ПКМ локальной ссылки вызывает `bridge.showPathMenu`. Web HTTP(S) открывается в браузере. Ошибки выводятся под Markdown-сообщением (`role=alert`), а не подавляются.
- Явный `urlTransform` пропускает локальные и HTTP(S)-адреса. Это устраняет прежнюю проблему: ReactMarkdown убирал drive-letter href, а UI regex отклонял относительные пути. `javascript:`, `data:`, `vbscript:` и произвольные схемы не передаются host; HTML не исполняется.
- `electron/file-links.mjs` нормализует `E:/...`, `/E:/...`, `E:\...`, относительные пути, URL-кодирование, `file:///`, суффиксы `:line`, `:line:column` и `#Lline`. Перед действиями realpath проверяет границы текущей папки, включая junction/symlink. Сетевые и служебные пути не поддерживаются.
- `openLink` использует Electron `shell.openPath` для файла/папки: Windows выбирает приложение, exe запускается по явному клику пользователя. Это заменяет прежнее поведение `openPath`, которое только показывало расположение. Не применяется командная оболочка и не собирается строка команды из ссылки.
- `showLocalPathMenu` создаёт нативное Electron Menu с пунктом «Открыть в проводнике». Выбор повторно проверяет путь и вызывает `shell.showItemInFolder`; отмена меню ничего не открывает. IPC по-прежнему проверяет окно/вкладку, scoped preload передаёт sessionId; действие использует cwd той вкладки, в которой показана ссылка.

Номер строки влияет только на разбор ссылки: открывается сам файл, без навигации к строке. Локальные ссылки вне проекта остаются недоступны. Наличие файла и права доступа проверяются при действии пользователя; отрисовка сообщения не открывает файлы.

## Проверки

`tests/file-links.test.mjs` проверяет нормализацию, существующие/отсутствующие файлы, границы проекта, вызовы shell и меню с подставными Electron API; реальные программы и exe не запускаются. `npm.cmd run test:links` проверяет настоящий renderer в Edge с подставными scoped bridges: click/Enter/ПКМ, форматы ссылок, запрет схем, ошибки и изоляцию вкладок. Скриншот: `artifacts/links-browser.png`.

Проверено: все 39 Node-тестов прошли; TypeScript/Vite и `test:links` прошли, включая отдельную регрессию корневых путей `README.md:12`, `main.py:20:4` и блокировку `javascript:123`. `npm.cmd run package` обновил стандартный `release/win-unpacked/Codex Desk.exe`; обработчики ссылок и renderer в app.asar сверены с собранными файлами. Настоящее открытие приложений и окна проводника тестами не выполнялось: shell/Menu подставные. Ограничение автоматизации Electron в текущей среде описано в [TABS.md](TABS.md).

## Исправление абсолютных ссылок с закодированным родительским каталогом

Пользователь воспроизвёл ошибку «Файл находится за пределами выбранного проекта» на `/E:/My%20projects/CodexDesk/artifacts/tabs-aligned.png`. Прежние тесты проверяли закодированное имя файла и parser, но не разрешение абсолютного пути с пробелом в родительской папке. `localLinkCandidates` правильно возвращал буквальный и декодированный пути, однако `resolveLocalLink` завершался при проверке первого (`E:\My%20projects\...`) до декодированного (`E:\My projects\...`).

Теперь недопустимый кандидат пропускается; каждый следующий отдельно проходит lexical/realpath-проверку границ. Ошибка границ возвращается, только если допустимый файл не найден. Буквальные `%20` имена по-прежнему поддержаны, ограничения выхода из проекта и junction сохранены. Новая регрессия сначала воспроизвела падение, после исправления весь набор из 40 Node-тестов прошёл. `test:links` и браузерные вкладки тоже прошли. Сквозная проверка реального preload/IPC — `scripts/ui-links-host.mjs`, с подставными shell/Menu, без запуска модели и внешних программ.

`ui-links-host.mjs` прошёл и с исходниками, и с готовым `release/link-path-fix/win-unpacked/Codex Desk.exe`. Проверена именно ссылка пользователя `/E:/My%20projects/CodexDesk/artifacts/tabs-aligned.png` через packaged preload/IPC: open/reveal получили существующий декодированный файл. Также проверены другое cwd, закрытая сессия, file URL/Unicode/суффиксы и отмена меню. Артефакт: `artifacts/links host-Sz9kty/result.json`. Native shell/Menu подставные, реальные приложения не запускались. Успешная сборка записана в `release/active-build.txt`, обычный ярлык уже следует этому пути.
